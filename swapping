#include <stdio.h>
int main() {
  int x,y,z;
  printf("Enter first number: ");
  scanf("%d", &x);
  printf("Enter second number: ");
  scanf("%d", &y);
  z = x;
  x = y;
  y = z;
  printf("After swapping, first number = %d", x);
  printf("After swapping, second number = %d", y);
  return 0;
}
